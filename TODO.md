# TODO

- [ ] Implement end-to-end f64 input and storage. Do not expose an f64 API that
  silently rounds inputs or stored vertices to f32. Scope and acceptance gates
  are tracked under “f64 input and output” in [ROADMAP.md](ROADMAP.md).
